//Given an array of integers, return a new array with each value doubled.

//For example:

//[1, 2, 3] --> [2, 4, 6]

function maps(x){ //create a funtion that takes an array
    return x.map(x => x*2) //map funcntion to return an array thats doubles
  }